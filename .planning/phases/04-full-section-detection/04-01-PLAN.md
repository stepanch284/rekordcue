---
phase: 04-full-section-detection
plan: 01
type: tdd
wave: 1
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - detect.py
  - tests/test_detect_section.py
autonomous: true
requirements:
  - DETECT-04
  - DETECT-05
  - DETECT-06
  - DETECT-07
  - DETECT-09
  - DETECT-12

must_haves:
  truths:
    - "detect_pssi_sections() reads PHRS tag from .EXT file and returns drop1_bar, breakdown_bar, drop2_bar, outro_bar (or None if missing)"
    - "detect_pssi_sections() returns confidence ~85% if PHRS present, ~0% if absent"
    - "detect_sections_from_energy() uses Simple Threshold + relative baselines (1.8x intro for drops, 0.6x for valleys)"
    - "detect_sections_from_energy() returns 4 sections + individual confidence scores (0-100)"
    - "All detected sections are snapped to 8-bar boundaries via bar_math.snap_to_8bar_boundary()"
    - "Confidence calibration: high energy ratio ≥2.5x baseline = 85%+, energy ratio <1.2x = <50%"
  artifacts:
    - path: "detect.py"
      provides: "PSSI phrase detection + Simple Threshold energy-based section detector"
      exports: ["detect_pssi_sections", "detect_sections_from_energy", "ENERGY_THRESHOLDS"]
    - path: "tests/test_detect_section.py"
      provides: "Unit tests for detection algorithms"
      min_lines: 80
  key_links:
    - from: "detect.py"
      to: "bar_math.py"
      via: "Uses snap_to_8bar_boundary() for final positioning"
      pattern: "snap_to_8bar_boundary"
    - from: "detect.py"
      to: "waveform.py"
      via: "Receives bar_energies from compute_bar_energies()"
      pattern: "compute_bar_energies"

---

<objective>
Create section detection module using PSSI phrases (when available) + Simple Threshold energy-based fallback. This is the core analysis engine that identifies Drop 1, Breakdown, Drop 2, and Outro positions.

Purpose: Enable accurate, tunable section detection across diverse DnB tracks with confidence scoring for user review.
Output: detect.py with 2 detection functions + comprehensive test suite + empirical threshold tuning documented
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/FEATURE_DECISIONS.md
@.planning/phases/04-full-section-detection/04-RESEARCH.md (algorithm comparison, recommendations)
@.planning/phases/03-beat-grid-bar-math/03-RESEARCH.md (bar_math context)
@bar_math.py (snap_to_8bar_boundary function)
@waveform.py (get_beat_grid, compute_bar_energies signatures)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Research tuning + create synthetic test suite for ground truth validation</name>
  <files>tests/test_detect_section.py (new), ground_truth_tracks.json (reference)</files>
  <read_first>
    - 04-RESEARCH.md (algorithm details, confidence scoring formula)
    - bar_math.py (use snap_to_8bar_boundary for final clamping)
    - Phase 3 bar_math tests (test pattern to follow)
  </read_first>
  <behavior>
    Create synthetic test cases + document ground truth thresholds:
    1. Test PSSI parsing: read sample .EXT files, verify PHRS tag extraction
    2. Test Simple Threshold detection:
       - Synthetic energy arrays: easy DnB structure (16-bar intro, drop at bar 16, breakdown at bar 48, drop 2 at bar 64)
       - Medium DnB structure (32-bar intro, drop at bar 32, breakdown at bar 56)
       - Tricky structure (8-bar intro, full-power roller with no breakdown)
    3. Test confidence scoring: high energy (3x baseline) → 90%+, ambiguous (1.2x) → 40-50%
    4. Test edge cases: track < 64 bars (incomplete structure), all-low-energy track
    5. Document tuning constants discovered: HIGH_THRESHOLD_RATIO = 1.8, LOW_THRESHOLD_RATIO = 0.6, etc.
  </behavior>
  <action>
Create `tests/test_detect_section.py` with 12+ test functions:
- test_pssi_read_valid: valid PHRS tag → sections populated
- test_pssi_read_missing_tag: no PHRS → returns empty dict
- test_pssi_confidence_present: PHRS sections get 85% confidence
- test_energy_detect_standard_16bar_intro: synthetic 16-bar intro, drop at bar 16
- test_energy_detect_32bar_intro: synthetic 32-bar intro, drop at bar 32
- test_energy_detect_full_power_no_breakdown: no valley detected → breakdown_bar = None
- test_energy_detect_short_track_incomplete: track < 64 bars → only returns available sections
- test_confidence_high_energy_peak: drop_confidence ≥ 85% when energy ≥ 2.5× baseline
- test_confidence_low_energy_valley: breakdown_confidence ≥ 80% when energy ≤ 0.6× baseline
- test_confidence_ambiguous_signal: ambiguous signal → confidence 40-60%
- test_snap_to_8bar_after_detect: detected section snapped to 8-bar boundary
- test_fallback_pssi_missing_uses_energy: if PHRS absent, falls back to energy

All tests import from detect module (ImportError expected on first run, RED phase).

Create reference file `ground_truth_setup.md` documenting:
- Threshold constants used
- Synthetic energy array generation
- Expected accuracy range (75-80% on diverse DnB)
  </action>
  <verify>
    <automated>cd C:/Users/stipecek/Documents/VisualStudioCode/novej && python -m pytest tests/test_detect_section.py -v 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_detect_section.py` exists with 12+ test functions
    - `grep -c "def test_" tests/test_detect_section.py` ≥ 12
    - `grep "from detect import" tests/test_detect_section.py` matches
    - Running pytest fails with ImportError (detect module doesn't exist yet, RED phase)
    - `ground_truth_setup.md` documents thresholds + synthetic generation
  </acceptance_criteria>
  <done>12+ unit tests + ground truth documentation created, all fail with ImportError (RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PSSI reader + Simple Threshold detector; tune thresholds empirically</name>
  <files>detect.py (new)</files>
  <read_first>
    - tests/test_detect_section.py (tests that must pass — GREEN phase)
    - 04-RESEARCH.md (algorithm pseudocode, confidence formulas)
    - bar_math.py (snap function signature)
    - waveform.py (ANLZ file access patterns from Phase 1-3)
  </read_first>
  <behavior>
    Implement in detect.py:
    1. helper function: `_read_phrs_tag(anlz_ext_file)` → list of (start_bar, end_bar, phrase_type) or empty list
    2. function: `detect_pssi_sections(db, content) → dict` with drop1_bar, breakdown_bar, drop2_bar, outro_bar (or None) + confidence
    3. helper: `_map_phrs_to_dnb_sections(phrs_list)` — map Rekordbox phrase types to DnB sections
    4. function: `compute_energy_baselines(bar_energies, intro_bars)` → (intro_baseline, high_threshold, low_threshold)
    5. function: `detect_sections_from_energy(bar_energies, bar_times_ms, intro_bars) → dict` with all 4 sections + confidences
    6. function: `compute_section_confidence(bar_energies, section_bar, section_type) → int 0-100`
    7. function: `detect_sections_hybrid(db, content, bar_energies, bar_times_ms, intro_bars) → dict` — try PSSI first, fallback to energy

    Key algorithm (Simple Threshold per 04-RESEARCH):
    ```python
    intro_baseline = mean(bar_energies[0:intro_bars])
    high_threshold = intro_baseline * 1.8  # tune: try 1.6-2.2
    low_threshold = intro_baseline * 0.6   # tune: try 0.5-0.7
    # For each bar: if energy > high_threshold and not drop1_found → drop1
    # Track sustained valleys (2+ bars) for breakdown detection
    ```

    Tuning constants (document in module docstring):
    ```python
    # ENERGY THRESHOLDS (tuned empirically on ground truth set)
    HIGH_THRESHOLD_RATIO = 1.8      # energy peak detection: x baseline
    LOW_THRESHOLD_RATIO = 0.6       # energy valley detection: x baseline
    MIN_VALLEY_DURATION_BARS = 2    # breakdown must sustain for 2+ bars
    OUTRO_ENERGY_RATIO = 1.2        # final 16 bars below this ratio = outro
    ```

    After implementing, run pytest: all 12+ tests must pass (GREEN phase).
  </action>
  <verify>
    <automated>cd C:/Users/stipecek/Documents/VisualStudioCode/novej && python -m pytest tests/test_detect_section.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def detect_pssi_sections" detect.py` matches
    - `grep "def detect_sections_from_energy" detect.py` matches
    - `grep "def detect_sections_hybrid" detect.py` matches
    - `grep "HIGH_THRESHOLD_RATIO = " detect.py` matches (tuning constants present)
    - `python -m pytest tests/test_detect_section.py -v` exits 0 (all tests pass)
    - `python -c "from detect import detect_pssi_sections, detect_sections_from_energy"` succeeds
  </acceptance_criteria>
  <done>PSSI + Simple Threshold detectors implemented, all 12+ tests pass (GREEN phase), thresholds tuned and documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ANLZ .EXT file → detect_pssi_sections | Local PHRS tag; no external input |
| bar_energies array → detect_sections_from_energy | Computed locally from waveform; no external data |
| bar_times_ms → final snapping | From PQTZ tag (Phase 3 grid offset corrected) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | PHRS tag parsing | accept | PHRS created by Rekordbox on local disk; no external source |
| T-04-02 | D (Denial) | loop over bar_energies | accept | O(n_bars) operation; n_bars < 300 for any track |
| T-04-03 | I (Info) | confidence scores | accept | Scores are non-sensitive metadata; lower scores signal uncertainty (OK to expose) |
</threat_model>

<verification>
1. `python -m pytest tests/test_detect_section.py -v` — all tests pass
2. `python -c "from detect import detect_pssi_sections, detect_sections_from_energy, detect_sections_hybrid"` — imports succeed
3. `grep "HIGH_THRESHOLD_RATIO\|LOW_THRESHOLD_RATIO\|MIN_VALLEY" detect.py` — tuning constants present
4. `grep -c "def " detect.py` — at least 7 functions (2 public, 5 helpers)
</verification>

<success_criteria>
- PSSI phrase reading works (or gracefully returns empty if no PHRS tag)
- Simple Threshold energy detection works with tuned thresholds
- Hybrid dispatcher tries PSSI first, falls back to energy
- All sections return 0-100 confidence scores
- All 12+ tests pass
- Ready for Phase 4.2 (main.py integration)
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-section-detection/04-01-SUMMARY.md`
</output>
